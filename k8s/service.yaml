apiVersion: v1
kind: Service
metadata:
  name: plant-health-service
  labels:
    app: plant-health-api
spec:
  type: NodePort
  selector:
    app: plant-health-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
